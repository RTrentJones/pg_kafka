# pg_kafka code audit — June 2026

A comprehensive, line-by-line audit of pg_kafka and the tracking record for its remediation.

- **Method:** 8 parallel deep readers, one per subsystem (protocol codec · runtime/dispatch · API
  handlers · consumer-group coordinator + assignment · Postgres storage · shadow-core · shadow-replay/
  license · config/error/tests). Each read its files in full and cross-checked call sites. ~51k lines
  covered (≈32k `src/` + ≈19k `kafka_test/`). The audit is **static** (no build/run); a few items that
  depend on `kafka-protocol 0.17` internals are marked *needs confirmation*.
- **Reconciliation:** the readers audited tree `d61b637` (17 commits behind `main` at the time). Only 4
  source files differed, and they were exactly this period's two fixes — **LeaveGroup v3+ `members[]`**
  (PR #18) and **Fetch capped to v11** (PR #19) — so those two findings are recorded as already-fixed
  and everything else stands against `main`. Line numbers below are as-audited; small shifts (≤ a few
  lines) are possible in `worker.rs`, `handlers/coordinator.rs`, and `response_builders.rs`.
- **Status legend:** 🔴 Open · 🟡 In progress · ✅ Fixed (PR #) · 🔵 Accepted / by-design (rationale) ·
  ⚪ Deferred.

This document is the **single source of truth** for remediation progress. Each fix PR flips its rows to
✅ and appends to the [resolution changelog](#resolution-changelog).

---

## Architecture in one screen

**A single-writer core.** A Postgres bgworker binds TCP on the main thread and spawns **one** tokio
network thread for I/O, framing (`LengthDelimitedCodec`, 100 MB cap), and codec. Every decoded request
is forwarded over a **bounded crossbeam channel (cap 10,000)** to **one synchronous DB thread**, which
runs all SPI inside a per-request `transaction()` + nested subtransaction. Responses flow back through
per-request channels, drained in **request-arrival order** so per-connection FIFO holds.

This single-writer decision is the spine of the design: it makes the "shared" store and the
`Arc<RwLock>` coordinator **effectively race-free** (network threads never call handlers), at the cost
of throughput — and it makes *any* blocking call on the DB thread a **global stall**.

- **Codec** delegates per-API decode/encode to the `kafka-protocol` crate; bespoke surface is the
  dispatch match, the request/response enums (`messages.rs`), a legacy MessageSet fallback, and the
  ApiVersions table.
- **Coordinator** is an in-memory group state machine (Empty→PreparingRebalance→CompletingRebalance→
  Stable); only committed offsets persist.
- **Storage** is one relational schema — a single `kafka.messages` table keyed by `(topic_id,
  partition_id, partition_offset)`, sibling tables for offsets/idempotence/txn. Offset assignment =
  `pg_advisory_xact_lock` + `MAX(offset)+1`. **SQL is fully parameterized — no injection found.**
- **Shadow mode** is a `ShadowStore<PostgresStore>` decorator that dual-writes/forwards to a real
  broker for migration. Large parts are incomplete or unwired.

---

## Status table

| ID | Sev | Area | Finding | Location | Status | PR |
|----|-----|------|---------|----------|--------|----|
| SEC-1 | 🔴 High | Security/DoS | Legacy MessageSet parser reads 6 bytes after an insufficient size check → panic on crafted Produce | `recordbatch.rs:198` | ✅ Fixed | #21 |
| SEC-2 | 🔴 High | Security/DoS | Request-header decode panics on truncated/empty frame (repo's own tests assert the panic) | `decoding.rs:45` | ✅ Fixed | #21 |
| SEC-3 | 🔴 High | Security/DoS | No concurrent-connection cap + 100 MB frames → OOM / fd exhaustion | `listener.rs` | ✅ Fixed | #23 |
| SEC-4 | 🔴 High | Security/DoS | Blocking crossbeam `send()` inside async tasks + blocking `notify_tx` on DB thread → runtime starvation | `listener.rs:499,582`, `worker.rs:838,896` | ✅ Fixed | #23,#53 |
| SEC-5 | 🟠 Med | Security/DoS | Decompression bomb — auto-decompress with no output-size cap | `recordbatch.rs:139` | ✅ Fixed | #41 |
| SEC-6 | 🟠 Med | Security/DoS | Unbounded `with_capacity(len)` from wire-supplied count → multi-GB alloc | `subscription.rs:87`, `member_assignment.rs:114` | ✅ Fixed | #21 |
| SEC-7 | 🟡 Low | Security | Default bind `0.0.0.0` + no wire auth (SASL is a non-goal) | `constants.rs:261` | 🔵 Accepted (warned #51) | #51 |
| SEC-8 | 🟠 Med | Security | Shadow SASL password GUC is world-readable (default GUC flags) | `config.rs:445` | ✅ Fixed | #32 |
| SEC-9 | 🔴 Critical | Security/DoS | **kafka-protocol body decoders `with_capacity` an attacker-controlled array-length prefix → multi-GB alloc that aborts the *process* (SIGABRT). Not catch_unwind-able. 1 crafted packet → whole-worker DoS.** Found by the PR #21 fuzz harness. Fixed via a patched `kafka-protocol` fork that caps the prealloc. | `decoding.rs` (crate `FetchRequest::decode` et al.) | ✅ Fixed | #27 |
| BUG-1 | 🟠 Med | Correctness | **Five** wrong txn/idempotent error-code constants ship to clients (audit said four; missed `TRANSACTIONAL_ID_NOT_FOUND` and the bogus `TRANSACTION_TIMED_OUT=94`) | `constants.rs` | ✅ Fixed | #22 |
| BUG-2 | 🟠 Med | Correctness | CRC32C never validated — corrupt batches silently persisted | `recordbatch.rs:204` | ✅ Fixed | #46 |
| BUG-3 | 🔴 High | Durability | Offset reuse after aborted-tail cleanup → monotonicity violated, HWM regresses | `postgres.rs:149,1660` | ✅ Fixed | #31 |
| BUG-4 | 🟠 Med | Durability | High-watermark regresses after aborted-tail cleanup (`MAX(partition_offset)+1`) | `postgres.rs:316` | ✅ Fixed | #39 |
| BUG-5 | 🟠 Med | Correctness | Handler panic is contained but no response is sent → client hangs to timeout | `worker.rs:601`, `dispatch.rs` | ✅ Fixed | #38 |
| BUG-6 | 🟠 Med | Correctness | OffsetFetch (fetch-all) swallows storage errors and returns success | `consumer.rs:228` | ✅ Fixed | #30 |
| BUG-7 | 🟡 Low | Correctness | Client record timestamps dropped; `TIMESTAMP` (no tz) skews emitted ms | `postgres.rs:176,262` | ✅ Fixed | #42 |
| BUG-8 | 🟡 Low | Correctness | Partitioner `% partition_count` div-by-zero in release (only `debug_assert`) | `partitioner.rs:33` | ✅ Fixed | #24 |
| BUG-9 | 🟠 Med | Correctness | Rejoining leader reported `is_leader=false` with empty member list | `coordinator.rs:194` | ✅ Fixed | #25 |
| CONF-1 | 🟠 Med | Conformance | OffsetCommit performs no generation/member validation (zombie commits) | `consumer.rs:17` | ✅ Fixed | #37 |
| CONF-2 | 🟡 Low | Conformance | `allow_auto_topic_creation` ignored — every requested topic auto-created | `metadata.rs:69` | ✅ Fixed | #36 |
| CONF-3 | 🟡 Low | Conformance | Metadata empty topics array treated as "all topics" | `decoding.rs` (parse_metadata) | ✅ Fixed | #33 |
| CONF-4 | 🟡 Low | Conformance | Admin no-op error codes wrong + weak topic-name validation | `admin.rs:45,205,289` | ✅ Fixed | #24,#48 |
| CONF-5 | 🟡 Low | Conformance | AddPartitionsToTxn accepts negative partition id | `transaction.rs:102` | ✅ Fixed | #29 |
| CONF-6 | 🟡 Low | Conformance | No request-version range enforcement before `decode(buf, version)` | `decoding.rs:239` | ✅ Fixed | #33 |
| CONF-7 | 🟡 Low | Conformance | ListOffsets timestamp lookup returns UNSUPPORTED_VERSION | `fetch.rs:289` | ✅ Fixed | #43 |
| CONF-8 | 🟡 Low | Conformance | Oversize request maps to CORRUPT_MESSAGE (should be MESSAGE_TOO_LARGE) | `error.rs:195` | ✅ Fixed | #22 |
| CG-1 | 🟠 Med | Consumer-group | Common-strategy selection is dead code; non-leader strategies get no partitions | `coordinator.rs:210` | ✅ Fixed | #35 |
| CG-2 | 🟠 Med | Consumer-group | "sticky" never rebalances overloaded members; mislabels itself cooperative | `sticky.rs:172` | ✅ Fixed | #40 |
| CG-3 | 🟡 Low | Consumer-group | Rebalance timeout hardcoded 5 min; ignores members' `rebalance_timeout_ms` | `coordinator.rs:360` | ✅ Fixed | #34 |
| CG-4 | 🟡 Low | Consumer-group | No `session_timeout_ms` bounds (negative → immortal member); no per-group member cap | `coordinator.rs:88` | ✅ Fixed | #28,#45 |
| CG-5 | 🟡 Low | Consumer-group | Empty groups never reclaimed (only admin DeleteGroups frees them) | `coordinator.rs:623` | ✅ Fixed | #45 |
| CG-6 | 🟡 Low | Consumer-group | RoundRobin resets cycle per topic instead of a single global pointer | `roundrobin.rs:83` | ✅ Fixed | #32 |
| CG-7 | 🟡 Low | Consumer-group | DescribeGroups/ListGroups `unwrap()` on a poisoned lock → worker crash | `handlers/coordinator.rs:277,367` | ✅ Fixed | #28 |
| SH-1 | 🟠 Med | Shadow | `ShadowConfig::validate()` never called in production | `config.rs:220` | ✅ Fixed | #57 |
| SH-2 | 🟠 Med | Shadow | Primary status cached for process lifetime; `refresh()` never called (failover) | `primary.rs:90` | ✅ Fixed | #57 |
| SH-3 | 🟡 Low | Shadow | Config reload never clears the cache → stale topic config | `store.rs:735` | ✅ Fixed | #57 |
| SH-4 | 🟠 Med | Shadow | Abandoned transaction leaks buffered records in RAM forever | `store.rs:1449` | ✅ Fixed | #57 |
| SH-5 | 🟠 Med | Shadow | `is_healthy()` runs a blocking 5 s metadata fetch on every produce | `store.rs:324` | ✅ Fixed | #57 |
| SH-6 | 🟠 Med | Shadow | ExternalOnly produce returns synthetic offset 0 + skips local storage | `store.rs:935` | ✅ Fixed | #59 |
| SH-7 | 🟡 Low | Shadow | `forwarded` metric double-counted in ExternalOnly non-txn | `store.rs:946` | ✅ Fixed | #58 |
| SH-8 | 🟡 Low | Shadow | ExternalOnly non-txn ignores `forward_percentage` (forwards 100%) | `store.rs:565` | ✅ Fixed | #58 |
| SH-9 | 🟠 Med | Shadow | Async forwards never counted; silent drop on full channel (at-most-once) | `store.rs:483,490` | ✅ Fixed | #58 |
| SH-10 | 🔴 High | Shadow | Replay SQL references nonexistent columns; replay non-idempotent | `replay.rs:283` | ✅ Fixed | #60 |
| SH-11 | 🟠 Med | Shadow | `ShadowForwarder`/`ReplayEngine` are dead/unwired relative to the live path | `forwarder.rs` | ✅ Fixed | #60 |
| SH-12 | 🟠 Med | Shadow | Forwarding producer can reorder/duplicate (idempotence off) | `producer.rs:104` | ✅ Fixed | #58 |
| SH-13 | 🟠 Med | Shadow | Partial ExternalOnly batch failure double-writes (external + local) | `store.rs:935` | ✅ Fixed | #59 |
| SH-14 | 🔴 High | Shadow | Sync forwarding `block_on` head-of-line-blocks the whole instance | `store.rs:496` | ✅ Fixed | #58 |
| SH-15 | 🟡 Low | Shadow | Local partition id forced onto the external topic (assumes identical partitioning) | `store.rs:533` | ✅ Fixed | #58 |
| SH-16 | 🟠 Med | Shadow/License | License gate is warn-only and format-only (any `"a:b"` is "valid") | `license.rs` | 🔵 Accepted | #57 (freemium) |
| QA-1 | 🟠 Med | Tests | Codecov ignores the riskiest files → patch gate gameable for the SPI/async layer | `codecov.yml` | ✅ Fixed | #48 |
| QA-2 | 🟠 Med | Tests | Property tests fuzz the `kafka-protocol` library, not pg_kafka's decoder | `tests/property_tests.rs` | ✅ Fixed | #27 |
| QA-3 | 🟡 Low | Tests | Some "error-path" E2E tests assert nothing (pass on `Ok` and `Err`) | `kafka_test/.../error_paths,negative` | ✅ Fixed | #49 |
| QA-4 | 🟡 Low | Quality | Byte-weighted backpressure module is dead code | `backpressure.rs` | ✅ Fixed | #44 |
| QA-5 | 🟠 Med | Quality | Notification subsystem unused → long-poll floors at a fixed 100 ms sleep | `pending_fetches.rs`, `listener.rs:558` | ✅ Fixed | #52 |
| QA-6 | 🟡 Low | Docs | Stale docs (deviations, shadow default, coverage claims) | `PROTOCOL_DEVIATIONS.md`, `CLAUDE.md` | ✅ Fixed | #50 |
| QA-7 | 🟡 Low | Correctness | `verify_schema` misses phase-9/10 + shadow tables → late runtime failures | `worker.rs:111` | ✅ Fixed | #47 |
| QA-8 | 🟡 Low | Correctness | Several "runtime" GUCs are effectively restart-only | `worker.rs:277` | ✅ Fixed | #47 |
| QA-9 | 🟡 Low | Quality | `error.rs` shadowed constructors discard context (`key:"unknown"`) | `error.rs:408` | ✅ Fixed | #44 |

## Re-audit follow-ups (independent 8-reader re-verification, 2026-06-30)

After all 60 fix PRs merged, an independent 8-reader re-audit re-verified every finding against `main`
(`d7623e0`). It **confirmed all 58 original findings are genuinely resolved** — each fix present in code
with a fail-before/pass-after regression test, and a report-honesty spot-check (8 findings × code + 6 ×
regression test) came back clean (no phantom ✅). It also surfaced the follow-ups below. **None were in
the original audit** — the original cited the *non-transactional* occurrences; the remediation fixed
those but left the matching *transactional* / produce-panic paths. Initially recorded as open per owner
decision (record-only), they were then **all scheduled and fixed** in PRs #62–#65 (each with a
fail-before/pass-after regression test, CI-green self-merge). The bulk shared one root cause: the
**transactional shadow-forward path had never been moved onto the durable outbox** that S2/S3 built for
the non-transactional path — PR #62 moves it there, which closes RA-1 and most of RA-2.

| ID | Sev | Area | Issue | Loc | Status |
|----|-----|------|-------|-----|--------|
| RA-1 | 🔴 High | Shadow | Transactional commit-forward still uses `send_sync`→`block_on` (60 s) on the single DB thread → a txn producer committing while the external broker is slow/down freezes **all** clients. SH-14 was fixed only on the non-txn path. | `shadow/store.rs commit_transaction`→`forward_single_record`→`producer.rs send_sync` | ✅ Fixed (#62) |
| RA-2 | 🟠 Med | Shadow | Transactional forward bypasses the outbox → best-effort/at-most-once (no `shadow_tracking` row, no retry; SH-9 non-txn only), forces the local `partition_id` onto the external topic (SH-15 non-txn only), and for an **external-primary** topic a failed txn forward is readable nowhere = silent non-delivery (SH-6 interaction). | `shadow/store.rs insert_transactional_records`/`commit_transaction` | ✅ Fixed (#62) |
| RA-3 | 🟠 Med | Correctness | Produce `acks≥1` hand-rolls its send and bypasses `dispatch_response`'s `catch_unwind`→error-response, so a handler panic (a PG ERROR surfaces through pgrx as a panic) on produce sends no response → client hangs to timeout. BUG-5 residual on the highest-volume write API. | `worker.rs` produce dispatch (~917–1008) | ✅ Fixed (#64) |
| RA-4 | 🟠 Med | Shadow | Outbox has no `retry_count` cap / dead-letter — a permanently-failing forward (e.g. oversize payload) re-dispatches every poll forever; and an orphaned row whose `kafka.messages` row was deleted (cleanup / replay-over-aborted) is re-claimed forever via the inner `JOIN` (claim → JOIN-miss → re-due). | `shadow/store.rs poll_and_forward_outbox`, `bootstrap.sql shadow_tracking` | ✅ Fixed (#63) |
| RA-5 | 🟡 Low-Med | Consumer-group | The ~1 s empty-group reaper (CG-5) drops a group's monotonic generation token while `validate_commit` is lenient on an unknown group (CONF-1), so a stale `OffsetCommit` from a lagging/partitioned consumer is accepted instead of fenced `UNKNOWN_MEMBER_ID`. | `coordinator.rs` sweep × `validate_commit` | ✅ Fixed (#65) |
| RA-6 | 🟡 Low | Mixed | Cleanup bundle: blocking `ack_tx.send` in async `run_shadow_forwarder` (should `try_send`); stale unused `sql/shadow_mode.sql` duplicate (drift hazard, carries an outdated replay); `is_external_primary` doc claims standby-false but only checks the GUC; in-RAM forward/failed metric over-counts on duplicate acks (persistent `shadow_metrics` is correct); `JoinGroupResponse.protocol_name` echoes the requester's first protocol, not the selected common one; `replay_shadow_messages` lacks a `txn_state` filter (can queue uncommitted/aborted rows); sync forward-before-commit can leave a phantom external record on rollback; `PROTOCOL_DEVIATIONS.md` ListOffsets table/section cosmetic inconsistency. | various | ✅ Fixed (#62/#63/#65) |

**Resolution (PRs #62–#65):** the primary fix routed the transactional commit-forward through the same
durable outbox as the non-txn path — `commit_transaction` now writes `shadow_tracking` rows for the
just-committed records and lets the existing poll forward them (PR #62). That one change closed RA-1 and
most of RA-2 (no `block_on`, at-least-once, re-partition by key, external-primary durably delivered). The
remaining items shipped as independent, smaller changes: RA-3 wrapped the produce dispatch in the same
panic→response pattern (`dispatch_produce`, PR #64); RA-4 added a `retry_count` cap and made the outbox
poll skip orphaned rows (PR #63); RA-5 fenced stale commits on an unknown group with
`UNKNOWN_MEMBER_ID` (PR #65); and the RA-6 cleanup bundle landed across #62/#63/#65. Each fix shipped
with a fail-before/pass-after regression test and CI-green self-merge.

---

## Post-remediation review (independent 5-reader bug + security sweep, 2026-06-30)

After the RA follow-ups merged, a final 5-dimension review swept `main` (`f3e97bc`) for bugs and
security risks — (A) RA-fix regression surfaces, (B) untrusted-input boundary / DoS, (C) storage /
transaction / durability, (D) consumer-group / conformance, (E) shadow forward path / secrets. Every
High/Critical claim was **independently verified against source** before recording (the Critical was
confirmed against the pinned `kafka-protocol` fork itself). **None of these were in the original audit
or the RA re-audit.** Recorded 🔴 Open per owner decision (record-only; not scheduled for fix).

Three themes: (1) **SEC-9 was fixed incompletely** — the decode allocation cap missed the record-batch
vector, the highest-volume path (RV-1). (2) **The transaction subsystem is the least-hardened area** —
a hanging-transaction bug and a missing read_committed LSO clamp (RV-2, RV-4) that both prior audits
missed, plus another "transactional twin" of a fix that only landed on the non-txn path (RV-5, echoing
RA-1/RA-2/RA-5). (3) Everything the RA work directly touched (outbox CTEs, the `validate_commit` fence,
the dispatch panic path, external-primary read cutover, secret GUC flags) verified **clean**.

`Verified`: ✅ = confirmed firsthand against the code during this pass; ○ = reviewer-reported, plausible,
not deep-verified.

| ID | Sev | Area | Issue | Loc | Verified | Status |
|----|-----|------|-------|-----|:---:|:---:|
| RV-1 | 🔴 Critical | Sec / DoS | Residual SEC-9: `records.reserve(record_count)` in the pinned `kafka-protocol` fork is uncapped (`record_count` validated only `>= 0`); the SEC-9 cap covers `Array`/`CompactArray`/headers but not the record-batch vector. One unauthenticated Produce with `record_count = i32::MAX` → ~300 GB reserve → `handle_alloc_error` → `abort()` (SIGABRT). It is an allocation abort, not an unwinding panic, so `parse_request`'s `catch_unwind` cannot contain it → whole bgworker dies. | fork `records.rs:479` via `recordbatch.rs:139` | ✅ | ✅ Fixed (#71) |
| RV-2 | 🔴 High | Txn | Producer re-init strands the previous epoch's `pending` records → hanging transaction. `get_or_create_transactional_producer` bumps the epoch + resets the txn row to `Empty` but never aborts the old-epoch `pending` rows; commit/abort require `state='Ongoing'` and filter by the new epoch, the timeout sweep scans `state='Ongoing'` + aborts by the current epoch, cleanup touches only `'aborted'` → old rows reclaimed by no path. `get_last_stable_offset = MIN(pending offset)` pins the LSO forever → read_committed stalls + storage leak. Kafka's `InitProducerId` is meant to abort the ongoing txn. | `postgres.rs:966,1421,1507,1710,1681` | ✅ | ✅ Fixed (#72) |
| RV-3 | 🔴 High | Correctness | `dispatch_produce`'s `on_success` (= `emit_produce_notifications`, which does SPI) runs inside `wrap_response`, which executes **outside** `dispatch_response`'s `catch_unwind`. A Postgres ERROR in the post-insert notification SELECT surfaces as an unguarded pgrx panic → no response sent (RA-3 client-hang reintroduced) and it unwinds into `run_request_in_subtransaction` → rolls back the already-successful produce. Narrower trigger than a general handler failure, but a real hole in the RA-3 fix. | `dispatch.rs:57,129`; `worker.rs:812,994` | ✅ | ✅ Fixed (#73) |
| RV-4 | 🟠 Med-High | Txn | read_committed fetch is not clamped to the LSO. `fetch_records_with_isolation` ReadCommitted filters only `txn_state IS NULL` and returns committed rows **≥ LSO**. With interleaved txns (low offset pending, higher offsets committed first), a consumer reads past the pending offset and never re-reads it when it commits → lost / reordered delivery. Real Kafka never returns records ≥ LSO to read_committed. Compounds RV-2. | `postgres.rs:1611`; `fetch.rs:112` | ✅ | ✅ Fixed (#72) |
| RV-5 | 🟡 Med | CG / Txn | `TxnOffsetCommit` bypasses the RA-5 generation/member fence. `validate_commit` has one production caller (`consumer.rs:34`, non-txn); `handle_txn_offset_commit` validates only the producer epoch, and the decoder drops `generation_id`/`member_id`. A zombie EOS consumer with a still-valid epoch can commit at a stale generation. Transactional twin of RA-5 / CONF-1. | `handlers/transaction.rs:238`; `decoding.rs` | ✅ | ✅ Fixed (#73) |
| RV-6 | 🟡 Med | Shadow | External-primary + RA-4 retry cap → a record "readable nowhere". External-primary suppresses all local reads (correct for cutover); RA-4 caps outbox retries at 10, so a deterministically-failing forward (payload over the broker's `max.message.bytes`, authz failure) is dead-lettered: present locally, suppressed from reads, never external = silent non-delivery. Only in shadow + external-primary mode. Two reviewers converged. | `shadow/store.rs:875,351` | ✅ | ✅ Fixed (#75) |
| RV-7 | 🟡 Med | Txn | Transaction timeout hardcoded to 60 s; per-txn `timeout_ms` (stored, DEFAULT 60000) is never enforced. A client with `transaction.timeout.ms > 60s` is force-aborted at 60 s; a shorter one lingers. Txn analogue of the fixed CG-3. | `worker.rs:767`; `postgres.rs:1711` | ✅ | ✅ Fixed (#74) |
| RV-8 | 🟡 Med | Txn | Idempotent sequence advanced *before* the insert. On a *returned* insert `Err` (SpiError→KafkaError, not a panic), the per-partition path returns `Ok`-with-error-code → the subtransaction commits the advanced sequence though records didn't insert → client retry classified `Duplicate` → acked but never persisted = silent loss. Narrowed: most insert failures panic and roll back together. | `handlers/produce.rs:124`; `postgres.rs:889` | ○ | ✅ Fixed (#74) |
| RV-9 | 🟡 Med | Sec / DoS | Connection cap bounds count (1024) but not aggregate reserved memory. `LengthDelimitedCodec` reserves the full declared frame length on the 4-byte prefix; 1024 × `MAX_REQUEST_SIZE` (100 MB) ≈ 100 GB reservable from ~4 KB of attacker input (partial SEC-3). | `listener.rs:437`; `constants.rs:147` | ✅ | ✅ Fixed (#78) |

**Low-severity bundle (RV-10, all reviewer-reported ○, recorded for completeness):**
- **doc/behaviour:** RA-5's `validate_commit` doc comment now contradicts the code, and the fence means every group is "unknown" after a bgworker restart → a rejoin/rebalance storm on restart (arguably correct, undocumented) — `coordinator.rs:640`.
- **robustness:** periodic maintenance txns (outbox poll, metrics flush, timeout sweep) aren't panic-guarded like the request path → a PG ERROR there restarts the whole bgworker — `worker.rs:590,629,764`. A panic before `response_tx` reaches the dispatch helper drops it unsent → client hang (latent; current prep is panic-free) — `worker.rs:966`.
- **txn/offset consistency:** ack sent before the outer COMMIT (non-durable ack window) — `worker.rs:683`; LSO fallback uses `MAX+1` not the monotonic counter → LSO < HWM after cleanup — `postgres.rs:1683`; `log_start_offset` hardcoded 0 — `fetch.rs:171`.
- **secrets/info:** ✅ **Fixed (#79)** — `ShadowConfig` derived `Debug` → SASL password cleartext if ever `{:?}`-formatted (latent; `Config` redacts, `ShadowConfig` didn't) — `config.rs:146`; `shadow_tracking.error_message` was `GRANT SELECT … TO PUBLIC` and sinks raw broker error strings (info disclosure; inconsistent with SEC-8) — `bootstrap.sql:338`; "idempotence absorbs duplicates" comments were misleading (the outbox is at-least-once) — `producer.rs:113`, `listener.rs:164`. *Resolution:* manual redacting `Debug` for `ShadowConfig` (username + password → `[REDACTED]`); column-level `GRANT` withholding `error_message` from PUBLIC; comments corrected to note idempotence only suppresses duplicates within a producer session (cross-restart replay is at-least-once).
- **time/misc:** `shadow_tracking.forwarded_at` / `transactions.started_at` are naive `TIMESTAMP` vs `NOW()` → ~1 h retry/timeout skew across DST (BUG-7 class) — `bootstrap.sql`; a dropped ForwardAck bypasses the retry cap → re-forward livelock (narrow) — `store.rs:1029`, `listener.rs:166`; unbounded per-connection long-poll task spawn — `listener.rs:541`.
- **CG conformance:** ✅ **Fixed (#80)** — no `session_timeout_ms` bounds (`INVALID_SESSION_TIMEOUT` never enforced) → a hostile 24-day timeout blocks rebalances — `coordinator.rs:92`; OffsetCommit accepted mid-rebalance (no `REBALANCE_IN_PROGRESS`) — `coordinator.rs:642`. *Resolution:* the JoinGroup handler now rejects a `session_timeout_ms` outside `[6s, 30min]` (Kafka's `group.{min,max}.session.timeout.ms` defaults) with `INVALID_SESSION_TIMEOUT` before touching group state; `validate_commit` fences an OffsetCommit that arrives while the group is Preparing/CompletingRebalance with `REBALANCE_IN_PROGRESS`. *Still open:* follower/leader protocol-name edge cases (self-healing, near-zero impact) — `handlers/coordinator.rs:107`, `coordinator.rs:510` (deferred to the RV-10 consistency PR).

**Verified clean (not re-reported):** the RA-5 fence + simple-consumer exemption, RA-6 protocol selection, the
outbox claim/finalize CTEs (no double-claim; orphan-skip doesn't starve; `apply_ack` metric gating), the
durable txn-commit outbox path (no `block_on`), external-primary's full six-path read cutover, secret GUC
flags (`SUPERUSER_ONLY | NO_SHOW_ALL`), `config::validate()` wiring, routing determinism, license honesty,
primary refresh, BUG-3/4 offset monotonicity, SEC-1/2/6 boundary hardening, CG-7 poison recovery, and the
assignment strategies.

**Recommended primary fix (when scheduled):** RV-1 first — cap the fork reserve (`.min(MAX_DECODE_PREALLOC)`
or `try_reserve`) + repin + a huge-`record_count` regression test; it is a remote unauthenticated
worker-kill. Then a transactional-correctness PR for RV-2 (abort the ongoing txn on re-init) + RV-4 (add
`AND partition_offset < lso` for ReadCommitted), and small closes for RV-3 (run `on_success` inside the
guard) and RV-5 (plumb generation/member into `handle_txn_offset_commit`).

### Second-pass addendum (independent re-review, 2026-06-30)

A second 5-reader sweep re-reviewed `main` (`11236fa`) on angles the first pass did not hunt
systematically — SQL/SPI injection, integer-overflow/offset math, concurrency/TOCTOU, dependency /
supply-chain / compression — plus an **adversarial refutation** pass whose only job was to disprove
RV-1..RV-5.

**Report integrity: RV-1..RV-5 all CONFIRMED.** The skeptic could not refute any of the five after a
genuine attempt; no severity changes. RV-5 was strengthened: under **KIP-447** (the modern EOS default,
e.g. Kafka Streams `exactly_once_v2`) the zombie keeps a valid producer epoch, so the dropped
group-generation fence is the *only* protection — the producer-epoch fence does **not** cover it.

**Swept clean (with evidence):** SQL/SPI injection — all ~68 SPI sites use bound `$n` params, no SQL is
built from wire strings, and member/group-instance ids never reach SQL (the coordinator is in-memory).
Concurrency/TOCTOU — the single-DB-thread serialization is *verified real* (the coordinator `Arc` and
shadow store never leave the DB thread; only channel ends cross to the network thread), so every
check-then-act is serialized; the one concurrent region (tokio tasks + crossbeam bridge) is correctly
built (`try_send` discipline, per-connection ordered response slots, identity-preserving acks). The
numeric core (offset assignment, partitioner, routing, legacy parser) is sound. **SEC-5 (decompression
bomb) is now confirmed bounded** — all five codecs cap at 256 MiB/batch (was "needs confirmation").
The exhaustive fork alloc sweep confirms **RV-1 is the only *reachable* uncapped site**.

| ID | Sev | Area | Issue | Loc | Verified | Status |
|----|-----|------|-------|-----|:---:|:---:|
| RV-11 | 🟡 Low-Med | Deps | `bytes@1.11.0` — RUSTSEC-2026-0007 / CVE-2026-25541: `BytesMut::reserve`'s unchecked `new_cap + offset` yields an OOB spare-capacity slice on `usize` overflow (release builds wrap; crate sets no `overflow-checks`). Not practically triggerable here (reserves bounded by the 100 MB frame + 256 MiB decompress caps) but a shipped direct dep. | `Cargo.lock` (bytes) | ✅ | ✅ Fixed (#68) |
| RV-12 | 🟡 Low-Med | Correctness | A negative wire `base_sequence` is never validated. On an idempotent producer, `validate_sequence(last=-1, base<0, count=1)` gives `batch_last ≤ last` → the `Duplicate` branch → `Ok(false)` → records **not inserted, `ERROR_NONE` returned** = silent data loss. Self-scoped (needs the producer's own `producer_id`; conformant clients send `base_sequence ≥ 0`). Distinct from RV-8. | `recordbatch.rs:76`; `storage/mod.rs:678` | ✅ | ✅ Fixed (#73) |
| RV-13 | 🟡 Low | Conformance | Producer epoch `epoch = epoch + 1` overflows the `SMALLINT` (i16) column at 32767 → InitProducerId then raises `smallint out of range` → `KafkaError::Internal`, failing that producer/txn id permanently. Kafka allocates a fresh `producer_id` on epoch exhaustion. Needs 32,767 re-inits. | `postgres.rs:754,973`; `bootstrap.sql:163` | ○ | ✅ Fixed (#77) |
| RV-14 | 🟡 Low | Deps | `rand@0.8.5`/`0.9.2` (RUSTSEC-2026-0097, unsound `ThreadRng` reseed under a custom `log` logger) and `anyhow@1.0.100` (RUSTSEC-2026-0190, `downcast_mut` UB after `context`). Neither precondition is met in the reachable path (pg_kafka logs via `tracing`; no `downcast_mut` on a context-wrapped error). Bump when convenient (`>=0.8.6`/`>=0.9.3`; `>=1.0.103`). | `Cargo.lock` | ○ | ✅ Fixed (#77) |
| RV-15 | 🟡 Low | Sec / DoS | Sibling of RV-1: the fork's low-level `Vec<u8>`/std-`String`/`CompactBytes`/`CompactString` decoders `vec![0; wire_len]` uncapped (up to ~2–4 GB zeroing-abort). Currently **unreachable** — no generated message field is typed `Vec<u8>`/`String` (verified across all 183 message files) and pg_kafka never calls the low-level `types::` decoders — so this is latent defense-in-depth, not a live vector. Cap alongside the RV-1 fix. | fork `types.rs:524,668,817` | ✅ | ✅ Fixed (#71) |

**Info (recorded, no action):** dev-test-only advisories `postgres-protocol@0.6.9` (RUSTSEC-2026-0179/0180)
and `tokio-postgres@0.7.15` (RUSTSEC-2026-0178) enter via `pgrx-tests` `[dev-dependencies]` only — **not in
the shipped `.so`**, and exercised only against a trusted local Postgres. Concurrency latent fragilities
(safe today only because single-threaded): the config-cache reload is a non-atomic `clear()`-then-refill
(`store.rs:1203`) and the metrics `snapshot()`-then-`store(0)` is a non-atomic RMW (`store.rs:1222`) —
both would race if ever shared with the network thread; `handle_sync_group` reads/computes/writes across
four separate coordinator lock acquisitions with SPI in between (consistent only under the single-thread
invariant). Dependency hygiene is otherwise clean (the fork is pinned by exact rev, one git dep, no
escaping path deps).

---

## Findings in detail

### Security / DoS — the untrusted-input boundary

The TCP listener parses attacker-controlled bytes; this is the weakest area.

- **SEC-1** `recordbatch.rs:198–206` — after `message_size` passes `message_size < 0 || remaining <
  message_size`, the code unconditionally reads 6 bytes (CRC + magic + attrs); a 12-byte `records`
  payload survives the guard then panics on `get_u32`. Reachable via any Produce that falls to the
  legacy MessageSet path.
- **SEC-2** `decoding.rs:45` — `decode_request_header_from_buffer` panics rather than returning `Err`
  on a short/empty frame (the original `test_parse_*_header_panics` tests asserted the panic). Fixed by
  wrapping the whole parse in `catch_unwind` so the boundary degrades to a CORRUPT_MESSAGE response; the
  two tests now assert a graceful `Err`. (Note SEC-9: `catch_unwind` only contains *unwinding* panics.)
- **SEC-3** `listener.rs` — the accept loop `tokio::spawn`s unboundedly (no semaphore) and each frame
  may be `MAX_REQUEST_SIZE = 100 MB`. Memory ≈ connections × 100 MB → trivial OOM / fd exhaustion.
- **SEC-4** `listener.rs:499,582` — blocking crossbeam `send()` from connection tasks; with only 2
  tokio workers, a full 10k channel lets two stuck sends starve the runtime. `worker.rs:838,896` do a
  blocking `notify_tx.send()` on the DB thread (should be `try_send`, as the shadow path already does).
- **SEC-5** `recordbatch.rs:139` — `RecordBatchDecoder::decode` auto-decompresses gzip/snappy/lz4/zstd
  with no output cap; the 100 MB limit bounds input only. *(needs confirmation re: crate internals.)*
- **SEC-6** `subscription.rs:87`, `member_assignment.rs:114` — `Vec/HashMap::with_capacity(topics_len)`
  where `topics_len` is validated only `>= 0`; a 6-byte JoinGroup claiming `0x7FFFFFFF` → multi-GB alloc.
- **SEC-7** `constants.rs:261` — 🔵 **Accepted (by-design).** `DEFAULT_HOST="0.0.0.0"` + no wire auth. Anyone reaching `:9092` can
  produce/consume and run DeleteTopics/DeleteGroups. (SASL is an explicit non-goal; this is about the
  *default* and documentation.)
- **SEC-8** `config.rs:445` — `SHADOW_SASL_PASSWORD` uses default GUC flags, so any DB role can `SHOW`
  the cleartext. Only the license key got `SUPERUSER_ONLY|NO_SHOW_ALL`.
- **SEC-9** *(found during PR #21 fuzzing — the most severe item in this audit)* — the `kafka-protocol`
  crate's per-API body decoders call `Vec::with_capacity(n)` on an `n` read straight from a wire array-
  length prefix, with no check against remaining bytes. A single crafted request (the fuzzer hit a Fetch
  with a huge `topics_len`) makes the decoder attempt a ~180 GB allocation, which calls
  `handle_alloc_error` → **`abort()` (SIGABRT)**. This is an allocation failure, **not** an unwinding
  panic, so `parse_request`'s `catch_unwind` cannot contain it: one small unauthenticated packet aborts
  the entire worker process. SEC-6 (our own pre-alloc caps) does not help because the allocation happens
  inside the dependency. Fix requires a pre-decode bound on array-length fields and/or a `kafka-protocol`
  upgrade to a version that uses `try_reserve`/bounded decoding; the full-pipeline fuzz harness is
  restored as the regression guard once this lands.

### Correctness / durability

- **BUG-1** `constants.rs` — **fixed in PR #22**, verified against Apache Kafka's `Errors`:
  `OUT_OF_ORDER_SEQUENCE` 47→**45**, `CONCURRENT_TRANSACTIONS` 53→**51**, `INVALID_TXN_STATE` 90→**48**,
  `PRODUCER_FENCED` 91→**90**, `TRANSACTIONAL_ID_NOT_FOUND` 79→**105** (a 5th the audit missed). Also
  removed the bogus `ERROR_TRANSACTION_TIMED_OUT=94` (94 is `INCONSISTENT_VOTER_SET`; Kafka has no
  timed-out code) and mapped `TransactionTimedOut`→`PRODUCER_FENCED`. They ship via
  `error.rs::to_kafka_error_code()`, so a retryable out-of-order sequence was reported as a fatal epoch
  fence.
- **BUG-2** `recordbatch.rs:204` — CRC explicitly skipped; corrupt batches accepted silently.
- **BUG-3** `postgres.rs:149,1660` — `partition_offset = MAX+1`; deleting aborted tail rows makes the
  next produce **reissue the same offsets** and moves the HWM backward. Violates offset monotonicity.
- **BUG-4** `postgres.rs:316` — HWM = `MAX(partition_offset)+1` with no `txn_state` filter (LSO at
  `:1573` is correct).
- **BUG-5** `worker.rs:601` — subtransaction rollback keeps the worker alive but `response_tx` is
  dropped unsent → the client hangs to its own timeout instead of `UNKNOWN_SERVER_ERROR`.
- **BUG-6** `consumer.rs:228` — fetch-all OffsetFetch logs and falls through to `error_code=NONE`.
- **BUG-7** `postgres.rs:176,262` — `Record.timestamp` never stored; `created_at` is `TIMESTAMP` (no
  tz) read via `EXTRACT(EPOCH …)`, TZ-skewed on non-UTC servers.
- **BUG-8** `partitioner.rs:33` — `% partition_count` guarded only by `debug_assert!`.
- **BUG-9** `coordinator.rs:194` — `is_leader = members.is_empty()`; a real leader rejoining a
  rebalance gets `is_leader=false` + empty member list. Masked because SyncGroup recomputes server-side.

### Protocol conformance

- **CONF-1** `consumer.rs:17` — OffsetCommit never checks `generation_id`/`member_id`.
- **CONF-2** `metadata.rs:69` — `allow_auto_topic_creation` ignored.
- **CONF-3** `decoding.rs` — empty topics array returns all topics (should be none).
- **CONF-4** `admin.rs:45,205,289` — DeleteGroups on missing group → NONE; CreatePartitions ==current →
  NONE; CreateTopics name validation empty-string-only.
- **CONF-5** `transaction.rs:102` — negative partition id passes (`< partition_count` only).
- **CONF-6** `decoding.rs:239` — version above advertised max passed straight to `decode`.
- **CONF-7** `fetch.rs:289` — ListOffsets timestamp → UNSUPPORTED_VERSION.
- **CONF-8** `error.rs:195` — oversize/short both → CORRUPT_MESSAGE.

### Consumer-group / assignment

- **CG-1** `coordinator.rs:210` — chosen protocol is just the leader's first strategy; others get no
  partitions.
- **CG-2** `sticky.rs:172` — balancing step skipped; mislabels `cooperative-sticky` while doing eager
  assignment.
- **CG-3** `coordinator.rs:360` — 5-min hardcoded rebalance timeout; `rebalance_timeout_ms` stored but
  unused.
- **CG-4** `coordinator.rs:88` — negative `session_timeout_ms` casts to huge `u128`; no member cap.
- **CG-5** `coordinator.rs:623` — empty group state lingers forever.
- **CG-6** `roundrobin.rs:83` — per-topic position reset imbalances heterogeneous subscriptions.
- **CG-7** `handlers/coordinator.rs:277,367` — `groups.read().unwrap()` crashes on a poisoned lock.

### Shadow mode

Shadow mode is a fire-and-forget mirror to a real broker for migration (reads pass through; writes
persist locally and forward). The docs imply a "compare pg_kafka vs production" validator — **that
comparison logic does not exist**, and the live path records nothing.

- **SH-1** `config.rs:220` — `validate()` only called by tests; clamping/normalization dead at runtime.
- **SH-2** `primary.rs:90` — first `pg_is_in_recovery()` cached forever; promoted standby never forwards.
- **SH-3** `store.rs:735` — reload `update()`s without `clear()`; deleted rows stay cached.
- **SH-4** `store.rs:1449` — timeout sweep never removes `pending_txn_messages` entries → RAM leak.
- **SH-5** `store.rs:324` — `is_healthy()` blocking metadata fetch per produce (per-record on commit).
- **SH-6** `store.rs:935` — ExternalOnly returns `Ok(0)` and skips local storage.
- **SH-7** `store.rs:946` — `forwarded` incremented twice in ExternalOnly non-txn.
- **SH-8** `store.rs:565` — `forward_records_required` never samples `forward_percentage`.
- **SH-9** `store.rs:483,490` — async path: successful forwards invisible to metrics; `try_send` drops
  silently on a full 10k channel.
- **SH-10** `replay.rs:283` — query uses `st.partition_offset`/`st.forwarded` but the table has
  `local_offset`/`external_offset`; replay also never advances progress → re-forwards everything.
- **SH-11** `forwarder.rs` — `ShadowForwarder`/`ReplayEngine` re-exported but never instantiated; the
  documented `kafka.replay_to_shadow()` SQL doesn't exist.
- **SH-12** `producer.rs:104` — `enable.idempotence=false` with retries and default in-flight → reorder/
  dupe within a partition.
- **SH-13** `store.rs:935` — ExternalOnly partial batch failure forwards some, then writes **all**
  locally → double-consume.
- **SH-14** `store.rs:496` — `send_sync` `block_on` on the single DB thread; broker down → ~2-min stall
  freezing all clients.
- **SH-15** `store.rs:533` — forwards pin the local `partition_id`; misroutes if the external topic has
  a different partition count.
- **SH-16** `license.rs` — `check_license()` only warns; `is_licensed()` never consulted; `"a:b"` is
  "valid" (no signature/expiry).

### Quality / tests / docs

- **QA-1** `codecov.yml` — ignores `worker.rs`, `listener.rs`, `storage/postgres.rs`,
  `shadow/store.rs`, `handler_context.rs`, `lib.rs`; the 90% patch gate is computed over pure/mocked
  logic and the SPI/async layer faces no requirement.
- **QA-2** `tests/property_tests.rs` — round-trips the `kafka-protocol` encoder / std; no random bytes
  reach pg_kafka's decoder.
- **QA-3** `kafka_test/.../error_paths,negative` — `test_produce_invalid_partition`,
  `connection_refused` pass on both `Ok` and `Err`.
- **QA-4** `backpressure.rs` — `ByteTracker`/`would_exceed_limit` never instantiated.
- **QA-5** `pending_fetches.rs`, `listener.rs:558` — `handle_fetch_long_poll` never calls `register()`;
  long poll degrades to `sleep(poll_interval)`, flooring latency at ~100 ms.
- **QA-6** `PROTOCOL_DEVIATIONS.md`/`CLAUDE.md` — "Transactions not planned" / Shadow under "Future
  Work" though both shipped; shadow default documented as PLAINTEXT but code defaults SASL_SSL.
- **QA-7** `worker.rs:111` — `verify_schema` checks only `topics`/`messages`/`consumer_offsets`.
- **QA-8** `worker.rs:277` — `compression_type`/`default_partitions`/`log_timing` read once into locals
  despite `Suset` context + "runtime" docs.
- **QA-9** `error.rs:408` — same-named constructor fns shadow variants; `InvalidConfig()` hardcodes
  `key:"unknown"`.

---

## What's genuinely strong

- **The single-writer model** makes the store and coordinator race-free by construction.
- **SQL is fully parameterized** — no injection; the single-table-keyed-by-`topic_id` schema avoids
  per-topic identifier interpolation.
- **Subtransaction rollback** (`run_request_in_subtransaction`) contains handler panics / Postgres
  ERRORs while keeping the one worker alive and discarding partial writes.
- **Error-vs-panic discipline in handlers** is good; the typed-error→code mapping is exhaustive
  (wildcard-free) and leaks no internal text to the wire.
- **Happy-path E2E is high quality** — exact message counts, gap-free offsets, watermarks, committed
  offsets; idempotent dedup, txn atomicity/isolation, group lifecycle exercised end-to-end.
- **Shadow DualWrite isolation is correct** — local write first via `?`; forwarding is best-effort and
  never corrupts the client's real produce.
- **The partitioner** correctly mirrors Kafka's `toPositive(murmur2(key)) & 0x7fffffff % n`.

## Already fixed this session

- **LeaveGroup v3+ `members[]` honored** — PR #18 (`e7282c3`). DeleteGroups-after-leave now works.
- **Fetch capped to v11** — PR #19 (`aa2f32d`). Avoids advertising the KIP-516 topic-id boundary the
  handler resolves by name.

---

## Remediation sequence

Themed PRs, priority order; each merged when CI is green, flipping its rows to ✅.

1. **Parser hardening** — SEC-1, SEC-2, SEC-6 ✅ (PR #21); SEC-5, BUG-2 deferred to 1b
1b. **Decoder allocation-bomb + CRC** — **SEC-9** ✅ + QA-2 ✅ (PR #27, via a patched `kafka-protocol`
   fork; full fuzz harness restored); SEC-5, BUG-2 → step 1c
2. **Error semantics** — BUG-1, CONF-8 ✅ (PR #22); QA-9 regrouped into step 11
3. **Runtime DoS & robustness** — SEC-3 ✅, SEC-4 (DB-thread half) ✅ (PR #23); SEC-4 (network half),
   BUG-5, QA-4 → step 3b
4. **Storage durability** — BUG-3,4,7
5. **Handler conformance** — BUG-8 ✅, CONF-4 (CreatePartitions + CreateTopics) ✅ (PR #24); CONF-1,2,3,
   CONF-4 (DeleteGroups), CONF-5,6,7, BUG-6 → step 5b
6. **Consumer-group correctness** — BUG-9 ✅ (PR #25); CG-1..7 → step 6b
7. **Shadow correctness** — SH-1..9,13,15
8. **Shadow durability + replay** — SH-10,11,12,14
9. **Shadow real license gate** — SH-16
10. **Security hardening** — SEC-7,8
11. **Test/coverage integrity** — QA-1,3,7,8
12. **Long-poll notifications** — QA-5
13. **Docs refresh** — QA-6

---

## Resolution changelog

_Appended as fixes land. Format: `YYYY-MM-DD — PR #N — IDs — summary`._

- 2026-06-28 — PR #20 — audit report created; all findings 🔴 Open.
- 2026-06-28 — PR #21 — parser hardening: **SEC-1** (legacy MessageSet panic), **SEC-2** (catch_unwind
  the whole parse), **SEC-6** (cap wire-driven `with_capacity`) → ✅. **QA-2** → 🟡 partial (legacy-parser
  fuzz added; full-pipeline fuzz deferred). Fuzzing surfaced a new **SEC-9** (🔴 Critical): the
  kafka-protocol body decoders abort the process on an allocation bomb — added to the table, fixed next.
- 2026-06-28 — PR #22 — error semantics: **BUG-1** (5 wrong error-code constants vs Apache Kafka's
  `Errors`; removed the bogus `TRANSACTION_TIMED_OUT=94` → maps to `PRODUCER_FENCED`) and **CONF-8**
  (oversize → `MESSAGE_TOO_LARGE`) → ✅, with tests pinning every value. **QA-9** regrouped into PR 11.
- 2026-06-28 — PR #23 — runtime DoS: **SEC-3** (connection cap via a `MAX_CONNECTIONS=1024` semaphore) →
  ✅; **SEC-4** DB-thread half (notify `send`→`try_send`) → ✅, network half → 🟡 deferred to step 3b
  (with BUG-5 and QA-4).
- 2026-06-28 — PR #24 — handler conformance: **BUG-8** (partitioner zero-guard) → ✅; **CONF-4**
  CreatePartitions no-op → `INVALID_PARTITIONS` and CreateTopics name validation → ✅, DeleteGroups
  arm → 🟡 deferred (needs store support, step 5b with CONF-1/2/3/5/6/7 and BUG-6).
- 2026-06-28 — PR #25 — consumer-group: **BUG-9** (a rejoining leader stays the leader; was demoted to
  a non-leader with an empty member list) → ✅, with a test. CG-1..7 → step 6b.
- 2026-06-28 — test backfill — added regression tests proving **SEC-3** (connection-admission cap +
  free-on-close, via an extracted `try_admit_connection` helper), **SEC-4** (`notify_best_effort` does
  not block on a full channel), and **SEC-6** (a huge wire array-length errors gracefully instead of
  OOM-aborting). Adopted test-per-bug as a standing rule for the remaining findings.
- 2026-06-28 — PR #27 — **SEC-9** (🔴 Critical) → ✅: the upstream `kafka-protocol` Array/CompactArray
  decoders pre-allocated from a wire-supplied length and aborted the process on a crafted huge array.
  Fixed by a patched fork (`RTrentJones/kafka-protocol-rs`, capped prealloc) pinned via
  `[patch.crates-io]`. Restored the full-pipeline `parse_request` fuzz + a deterministic huge-Fetch
  test as regression guards (**QA-2** → ✅). SEC-5/BUG-2 (decompression bomb / CRC) → step 1c.
- 2026-06-28 — PR #28 — consumer-group sweep: **CG-7** (DescribeGroups/ListGroups recover from a
  poisoned lock instead of crashing) → ✅, with a poison test; **CG-4** (negative `session_timeout_ms`
  no longer makes a member immortal) → 🟡 partial (per-group member cap still open). CG-1/2/3/5/6 → 6b.
- 2026-06-28 — PR #29 — **CONF-5**: AddPartitionsToTxn now rejects a negative partition id
  (UNKNOWN_TOPIC_OR_PARTITION) instead of accepting it as ERROR_NONE; added a txn-handler test module.
- 2026-06-28 — PR #30 — **BUG-6**: OffsetFetch fetch-all now surfaces a storage failure as a
  top-level error_code instead of returning a NONE/empty success (which a consumer reads as
  "no committed offsets" and may reset from); added a consumer-handler test module.
- 2026-06-28 — PR #31 — **BUG-3** (🔴 High): offsets are no longer reused after
  cleanup_aborted_messages deletes the highest rows. Added a never-decremented per-partition counter
  table (`kafka.partition_offsets`); both produce paths now assign `GREATEST(counter, MAX+1)` and
  advance it. E2E regression: produce → SQL-delete the tail → produce → assert offset = last+1, not
  reused. BUG-4 (HWM, semantically subtle) and BUG-7 (timestamps/TIMESTAMPTZ) → step 4b.
- 2026-06-28 — PR #32 — **SEC-8**: locked down the shadow SASL-password GUC with
  `SUPERUSER_ONLY | NO_SHOW_ALL` (mirroring the license key), with an E2E test proving a non-superuser
  role cannot `SHOW` it → ✅. **SEC-7** (0.0.0.0 default + no auth) → 🔵 Accepted: a broker binds all
  interfaces by design and SASL is a stated non-goal; mitigation is network controls + documentation.
  Also **CG-6**: RoundRobin now uses a single global pointer across all (topic, partition) pairs (was a
  per-topic counter), with a multi-topic regression test (bundled for patch coverage of the config change).
- 2026-06-28 — PR #33 — decoder conformance: **CONF-3** — Metadata now honours Kafka's version-aware
  empty-vs-null semantics (v1+ empty array → no topics, null → all topics; v0 empty → all topics),
  instead of treating every empty array as "all topics"; tests for empty@v9, empty@v0, null@v9.
  **CONF-6** — inbound request versions are now enforced against a single source-of-truth
  `API_VERSION_RANGES` table (used both to advertise *and* enforce); a version outside the advertised
  range returns `UNSUPPORTED_VERSION` before the version-specific body decode (ApiVersions exempt so
  clients can still negotiate down). Refactored `build_api_versions_response` to derive from the table.
  This makes the broker reject versions it doesn't advertise (correct Kafka behaviour), which is a no-op
  for conformant clients since they negotiate down via ApiVersions; four unit tests that decoded a
  version one above the advertised max (incl. the AddPartitionsToTxn v4 batch format) were realigned —
  three now exercise the advertised max, and the v4 one asserts the rejection.
- 2026-06-28 — PR #34 — **CG-3**: a stuck rebalance is now aborted after the group's effective
  rebalance timeout (the max of members' advertised `rebalance_timeout_ms`, as Kafka does) instead of a
  hardcoded 5 minutes; falls back to the 5-minute default only when no member advertises a positive
  value. Unit test drives a 20ms-budget rebalance to timeout in ~50ms (old hardcode would not fire).
- 2026-06-28 — PR #35 — **CG-1**: the group protocol is now the most-preferred strategy supported by
  *every* member (Kafka's common-protocol rule), via a new `select_group_protocol` that walks the
  leader's preference order and skips strategies a follower lacks; previously it took the leader's first
  strategy outright, handing a follower a server-computed assignment for a strategy it never advertised.
  Falls back to the leader's first only when members share none. Unit test: leader prefers sticky→range,
  a follower supports only range, group selects range.
- 2026-06-29 — PR #54 — **CONF-6 regression fix** (Sarama): the regenerated conformance evidence
  surfaced that **Sarama (Go)** fails `NewClient` — a regression from CONF-6's request-version
  enforcement (#33), not a pre-existing gap. Sarama `V2_8_0_0` is configured with an explicit broker
  version and does **not** down-negotiate, so it sends bootstrap **Metadata v10**; pg_kafka advertised
  Metadata max **9** and CONF-6 rejected v10 with the hand-rolled `KafkaResponse::Error` frame (v0
  header), which the encoder's own comment warns "strict clients may fail to parse." Sarama decoded
  that ~57-byte error as the flexible Metadata v10 response it expected and panicked (`slice bounds
  out of range [:120] with capacity 57`). Pre-CONF-6 pg_kafka leniently served v10, so it worked.
  Fix (the "advertise what we serve" approach): raise the advertised+enforced Metadata max **9 → 10**
  — pg_kafka serves a v10 response (by name; the v10 `topic_id` is left as the zero UUID, which by-name
  clients ignore). A full audit of Sarama V2_8_0_0's per-API versions confirmed Metadata v10 is the
  *only* request exceeding pg_kafka's advertised ranges (Fetch=11, Produce=7, OffsetCommit=7,
  InitProducerId=4, group/admin/txn all ≤ advertised), so this single cap raise restores Sarama. The
  CONF-6 decoder tests now reject v11 and accept v10. Verified by re-dispatching `evidence.yml`.
- 2026-06-29 — PR #53 — **SEC-4** (completes #23): the network-side request handoff no longer blocks
  the async runtime. The connection handler and the long-poll re-fetch sent each parsed request to
  the DB thread with a *blocking* crossbeam `Sender::send`; when the bounded request channel is full
  (DB thread behind), that parks the whole tokio worker thread and starves every other connection
  scheduled on it. Replaced both sites with `send_with_backpressure`, which `try_send`s and, on a full
  channel, `await`s a 1ms sleep and retries — yielding the worker to other tasks. This preserves the
  bounded channel's backpressure (the request is never dropped or reordered; we wait for room) while
  keeping the runtime responsive; the not-full path is a single `try_send` with no sleep. Two unit
  tests cover the helper (waits-then-succeeds-in-order; reports-disconnect). The DB-thread half (notify
  `send`→`try_send`) landed in #23, so SEC-4 is now fully closed.
- 2026-06-29 — PR #52 — **QA-5**: long-poll fetches now wake on a produce notification instead of
  only the fixed 100 ms poll interval. The notification path was already 90% wired (DB thread sends
  `NewMessages`, the network thread runs a notifier task that calls `registry.notify_new_data`), but
  the fetch handler never registered a waiter (`_registry` was unused) and just `sleep`ed — the
  blocker being that the handler has the topic *name* while the registry was keyed by topic *id*,
  which the network thread can't resolve without an SPI round-trip. Fixed by keying the registry by
  topic **name** (it travels on the notification — the produce path already has it) and having
  `handle_fetch_long_poll` register every requested partition on one shared `Notify`, then
  `select!` on the notification vs. the poll interval. **The poll interval is retained as a
  correctness fallback**, so a missed wake-up degrades to exactly the old fixed-interval behaviour
  rather than stalling — notifications are a pure latency win. `register` now takes a caller-supplied
  `Notify` (one per fetch, shared across its partitions); the registry's unit tests were updated and a
  name-keyed `test_notify_wrong_topic` added. Touches `notifications.rs`/`pending_fetches.rs` (unit-
  tested) + `worker.rs`/`listener.rs` (E2E `long_poll` category).
- 2026-06-29 — PR #51 — **SEC-7** (hardening; stays 🔵 accepted-by-design): the no-wire-auth +
  `0.0.0.0` default is intentional (SASL is a non-goal), but it was *silent*. It is now loud: a new
  pure helper `config::bind_is_publicly_exposed(host)` (unit-tested — loopback `127.0.0.0/8`/`::1`/
  `localhost` = not exposed; `0.0.0.0`/`::`/empty/any concrete LAN address = exposed) drives a startup
  `WARNING` from the bind path in `worker.rs` whenever the listener binds to a non-loopback interface,
  stating that pg_kafka performs no Kafka-wire auth and must be protected by network controls or bound
  to `127.0.0.1`. The `pg_kafka.host` GUC description and a new "Network Security Posture (SEC-7)"
  section in `PROTOCOL_DEVIATIONS.md` document the accepted posture + mitigation. Default bind is
  unchanged (loopback-only would break the common multi-host broker setup).
- 2026-06-29 — PR #50 — **QA-6**: refreshed the stale lower half of `PROTOCOL_DEVIATIONS.md`, which
  still contradicted its own (correct) summary and `KAFKA_PROTOCOL_COVERAGE.md`. "Notable Missing
  APIs / Transaction APIs (22-26) – Not planned" and "Future Work / Shadow Mode (Future Phase)" were
  removed (both shipped — Phase 10 + Phase 11); "Exactly-once semantics – May Not Implement" corrected
  (EOS is implemented); ListOffsets §8 flipped from "timestamp lookup not implemented → UNSUPPORTED_
  VERSION" to the implemented behaviour (earliest offset whose record timestamp ≥ target; BUG-7 +
  CONF-7); and the client-config snippet's `enable.idempotence=false # REQUIRED` corrected to supported
  (Phase 9). Fixed `CLAUDE.md`'s shadow GUC comment, which mislabelled the default as PLAINTEXT — the
  code default is `SASL_SSL` (`config.rs` `test_config_default_shadow_security_protocol`). Docs only.
- 2026-06-29 — PR #49 — **QA-3**: the four assert-nothing negative E2E tests now carry real
  assertions (they previously printed an outcome and returned `Ok` on both the `Ok` and `Err` branch,
  so they could never fail). `test_connection_refused` fails if a produce to a dead port (`:19999`)
  succeeds; `test_produce_timeout` fails unless a 1ms `message.timeout.ms` — well below a real
  network+SPI produce — trips at least one local delivery timeout; `test_invalid_group_id` now seeds a
  record and fails if a consumer with an empty `group.id` ever delivers it (the empty id must be
  rejected at create/subscribe or never consume); `test_duplicate_consumer_join` fails unless both
  members remain subscribed to the topic after the second join rebalances. All four return `Err` on
  failure (not `assert!`) so a regression surfaces as a single failed test rather than unwinding the
  suite. The SEC-8 `test_sasl_password_guc_is_superuser_only` in the same file already asserts.
- 2026-06-29 — PR #48 — **QA-1** + **CONF-4** (coverage policy + DeleteGroups decision): **QA-1** —
  `codecov.yml` now states the coverage policy explicitly: the unit gates apply only to code runnable
  without a live PostgreSQL; the ignored SPI/async/pgrx files are mandatorily covered by kafka_test
  E2E regression tests (the test-per-bug rule this remediation applied throughout), and adding a file
  to the ignore list to dodge the patch gate is disallowed. Documents the limitation + its mitigation
  rather than leaving a silent dodge. **CONF-4** — the admin no-op error codes and topic-name
  validation were fixed in #24; the remaining "DeleteGroups on a missing group returns NONE" arm is
  **accepted by design**: idempotent DeleteGroups is required for CG-5 (which reclaims empty groups), so
  that a clean leave → delete cannot flakily race the reclaim. (Kafka keeps Empty groups deletable; we
  reclaim them but keep delete idempotent, which is the compatible choice.)
- 2026-06-29 — PR #47 — **QA-7** + **QA-8** (worker startup correctness): **QA-7** — `verify_schema` now
  checks all 12 runtime tables (was just topics/messages/consumer_offsets), so a partial/stale schema
  missing the phase-9/10 idempotent+transaction tables, the BUG-3 offset counter, or the shadow tables
  is caught at worker start instead of as a late SPI failure. **QA-8** — the seven non-shadow GUCs the
  worker reads once at startup (log_connections, shutdown_timeout_ms, default_partitions,
  fetch_poll_interval_ms, enable_long_polling, compression_type, log_timing) are relabelled `Suset` →
  `Postmaster`, so a runtime `SET` fails loudly instead of silently no-op'ing; the `log_connections`
  description that falsely claimed "can be changed at runtime" is corrected.
- 2026-06-29 — PR #46 — **BUG-2**: record-batch CRCs are validated. The live RecordBatch v2 path
  already has its CRC32C checked by the kafka-protocol decoder; the legacy MessageSet v0/v1 fallback
  read the CRC32 and ignored it (`let _crc = …`). It now validates it (dep-free bit-by-bit CRC-32/IEEE
  over the message body) and stops parsing on a mismatch rather than persisting a corrupt message — so
  no batch is silently persisted with a bad checksum on either path. (The legacy fallback is itself
  off the live path now that CONF-6 rejects Produce v0–2.) Tests embed real CRCs; a deliberately wrong
  CRC is rejected.
- 2026-06-29 — PR #45 — **CG-4** (completes #28) + **CG-5** (consumer-group lifecycle): **CG-4** caps a
  group at `MAX_MEMBERS_PER_GROUP` (1000) — a genuinely new member is rejected with
  `GROUP_MAX_SIZE_REACHED` (a rejoining member is always allowed); this finishes the partial fix (the
  negative-session-timeout bound landed in #28). **CG-5** reclaims empty groups on every periodic sweep
  (`check_and_remove_timed_out_members`) instead of only via the next join_group's 24h TTL pass — an
  empty group holds no durable state (offsets are in storage) and DeleteGroups-on-a-missing-group is
  idempotent, so this is safe. Unit tests: the 1001st member is rejected; a sole-member timeout leaves
  the group reclaimed (None). `test_member_timeout_detection` updated from "empty" to "reclaimed".
- 2026-06-29 — PR #44 — **QA-4** + **QA-9** (dead/misleading code removal): deleted the byte-weighted
  backpressure module (`backpressure.rs` + its re-export) — it was never wired in (the request channel
  is count-bounded); the `approx_byte_size` estimators are kept as request-size utilities with the
  stale "backpressure" comments corrected. Removed the legacy `KafkaError::InvalidConfig(message)`
  constructor that fabricated `key: "unknown"` (and its test, which asserted that discarded context);
  callers use the struct form with the real key.
- 2026-06-28 — PR #43 — **CONF-7**: ListOffsets with a real timestamp now resolves to an offset
  instead of returning `UNSUPPORTED_VERSION`. A new `get_offset_for_timestamp` store method returns the
  earliest committed offset whose stored record timestamp (BUG-7) is at or after the target (and that
  record's timestamp), or `None` → offset -1 per the protocol. Wired through the trait, Postgres impl,
  shadow delegate, and mock; the handler's timestamp arm calls it. Handler unit test (mocked lookup →
  offset+timestamp) + E2E (`offsets_for_times` over three timestamped records resolves to the right
  offsets). Depends on BUG-7's stored timestamps.
- 2026-06-28 — PR #42 — **BUG-7**: the producer's record timestamp is now persisted and returned to
  consumers instead of being replaced by the broker's insert time. `kafka.messages` gains a
  `timestamp_ms BIGINT` column written by both produce paths (`r.timestamp.unwrap_or(-1)`); the fetch
  emits it directly, falling back to `created_at` only when a record carried no timestamp (-1).
  `created_at` is now `TIMESTAMPTZ` (was naive `TIMESTAMP`), so the fallback's `EXTRACT(EPOCH ...)` no
  longer skews by the session time zone. E2E: produce with a fixed past CreateTime → the consumed
  message carries the same epoch-ms (previously it saw the server insert time). Unblocks CONF-7.
- 2026-06-28 — PR #41 — **SEC-5**: decompression bombs are now bounded. The `kafka-protocol` fork
  (already patched for SEC-9) gained a 256 MiB `MAX_DECOMPRESSED_SIZE` cap enforced across all four
  codecs: gzip/lz4/zstd stream through a `BoundedWriter` that errors past the cap, and snappy rejects a
  raw header (or accumulated framed blocks) whose declared uncompressed length exceeds it — before
  allocating. pg_kafka repins the fork (`bounded-decompress`, 703af18). The cap's bomb-rejection is
  unit-tested in the fork (a raw-snappy header declaring ~512 MiB is rejected); pg_kafka's existing
  compression E2E suite (gzip/lz4/snappy/zstd produce+consume) proves legitimate batches still decode.
- 2026-06-28 — PR #40 — **CG-2**: the sticky assignor now actually rebalances and stops mislabelling
  itself. Step 4 (previously a "skip for MVP" no-op) now moves partitions from overloaded members to
  subscribed, less-loaded members until the load spread is ≤ 1 — so a new member joining an assignment
  one member fully owns gets a fair share (e.g. 4/0 → 2/2) instead of nothing. `name()` now reports
  `"sticky"` (eager) rather than `"cooperative-sticky"`: pg_kafka computes a full server-side
  assignment and does not implement the cooperative incremental (KIP-429) protocol, so claiming it
  could make a client skip revoking moved partitions and briefly double-own them. The coordinator
  still resolves either alias via `contains("sticky")`. Unit test flips the old 4/0 assertion to 2/2.
- 2026-06-28 — PR #39 — **BUG-4**: the high watermark no longer regresses after aborted-tail cleanup.
  `get_high_watermark` returned `MAX(partition_offset)+1`, which drops below already-issued offsets
  once `cleanup_aborted_messages` deletes the highest rows. It now returns
  `GREATEST(kafka.partition_offsets.next_offset, MAX+1)` — the same never-decreasing per-partition
  counter produce assigns from (BUG-3) — so the HWM is the true monotonic log-end. Read isolation is
  unchanged (decided by the fetch row filter + last-stable-offset, not the HWM bound). _The audit
  framed this as "HWM counts pending/aborted," but in Kafka the HWM legitimately includes those
  physically-present records; the real defect is the post-cleanup regression._ E2E: produce 5 →
  delete the top 2 rows → `fetch_watermarks` high stays at 5 (old code reported 3).
- 2026-06-28 — PR #38 — **BUG-5**: a handler panic no longer hangs the client until its request
  timeout. `dispatch_response` now catches the panic, replies `UNKNOWN_SERVER_ERROR`, then re-raises
  the original payload so the surrounding subtransaction still rolls back the partial writes
  (`run_request_in_subtransaction` flushes the PG error state + rolls back on the re-raised panic). The
  catch branch does only Rust work (channel send + response build), so it is safe before
  `FlushErrorState`. Unit test: a panicking handler yields an `UNKNOWN_SERVER_ERROR` response *and* the
  panic propagates.
- 2026-06-28 — PR #37 — **CONF-1**: OffsetCommit now validates the committing member against the live
  group, rejecting zombie commits. The request's generation + member id (previously dropped at parse)
  are threaded through `KafkaRequest::OffsetCommit` to the handler, which calls a new
  `GroupCoordinator::validate_commit`: a stale generation → `ILLEGAL_GENERATION`, an unknown member →
  `UNKNOWN_MEMBER_ID` (per-partition). A simple consumer (`generation_id < 0`) and a group this
  coordinator has no record of are not validated — mirroring the existing `heartbeat` validation, so no
  regression for live clients (which negotiate generations). Coordinator + handler + decoder unit tests.
- 2026-06-28 — PR #36 — **CONF-2**: Metadata now honours the v4+ `allow_auto_topic_creation` flag.
  Previously every specifically-requested topic was auto-created, so a client merely *probing* whether a
  topic exists would silently create it. The flag is now carried through `KafkaRequest::Metadata` to the
  handler; when false, a missing topic resolves via a non-creating `get_topic_metadata` lookup and is
  reported as `UNKNOWN_TOPIC_OR_PARTITION` instead of being created (the flag defaults to true, so v0–3
  and auto-creating clients are unchanged). Handler unit test asserts no-create + the error code; decoder
  test asserts the flag is parsed.
- 2026-06-29 — PR #57 — shadow correctness sweep (PR S1 of the shadow remediation): **SH-1** run
  `ShadowConfig::validate()` on the producer (re)create path (was test-only); **SH-2** new
  `refresh_primary_status()` on the worker reload cycle so a promoted standby starts forwarding;
  **SH-3** reload now *replaces* the topic cache (clear + repopulate) on a successful query so a deleted
  `shadow_config` row stops forwarding, and returns `DatabaseError` (keeping the live cache) on a failed
  query; **SH-4** `pending_txn_messages` buffers carry a `first_buffered` stamp and the
  `abort_timed_out_transactions` override evicts buffers older than the sweep timeout (RAM-leak fix);
  **SH-5** `is_healthy()` gated behind a 30 s TTL instead of a blocking metadata fetch per produce → ✅.
  **SH-16** → 🔵 Accepted (freemium by design): kept warn-only/format-only but made the code honest —
  documented the honor-system model, renamed `LicenseStatus::Valid`→`Registered`, dropped the false
  "future ed25519 verification" promise. Tests: unit (SH-4 eviction) + shadow E2E
  (`test_reload_clears_deleted_topic_config`, SH-3). The forward path / lossy channel is untouched here —
  that is PR S2 (durable outbox).
- 2026-06-29 — PR #58 — durable forwarding outbox (PR S2, the shadow core): **SH-9** replaced the lossy
  in-RAM forward channel with an at-least-once outbox on `kafka.shadow_tracking` — a produce writes a
  pending pointer row in the same txn; a DB-thread poll claims due rows (claim-and-return CTE), the
  network thread forwards via the idempotent producer and returns a `ForwardAck`, and the poll finalizes
  the row. **SH-14** bounded-sync (write row, wait ≤5s for the ack, return; no `block_on`). **SH-12**
  idempotent producer (`enable.idempotence`, `max.in.flight=5`, `acks=all`). **SH-7** metric bumped in
  the same statement that flips the row (`external_offset IS NULL` guard → exactly-once). **SH-8**
  percentage applied when selecting outbox rows. **SH-15** forward with `partition=None` (re-partition
  by key) → ✅. Shadow E2E `test_outbox_row_written_and_finalized` + the existing suite now run through
  the outbox.
- 2026-06-29 — PR #59 — external-primary read cutover (PR S3a): **SH-6** ExternalOnly redefined as
  external-primary — the write path is now identical to DualWrite (persist locally + durable outbox) and
  the differentiator is READS: `fetch_records`/isolation return empty and the watermark/offset lookups
  report an empty partition for external-primary topics, so consumers cut over to the external broker
  (delivery from one source). **SH-13** deleted the synthetic-offset / skip-local / partial-failure
  double-write in both the non-txn and transactional paths; ExternalOnly writes locally exactly like
  DualWrite. Config loader forces `forward_percentage=100` for external-primary (a sampled-out record
  would be readable nowhere). Removed the dead `forward_records_required` → ✅. Shadow E2E
  `test_external_only_mode` asserts external has it, the local table has it, and a pg_kafka consumer
  reads zero. SH-10/SH-11 (replay + dead ShadowForwarder/ReplayEngine) → PR S3b.
- 2026-06-29 — PR #60 — replay via the outbox + dead-code removal (PR S3b, closes the shadow set):
  **SH-10** replaced the broken replay query (referenced a nonexistent `st.forwarded` column, never
  advanced progress) with a single SQL function `kafka.replay_shadow_messages(topic_id, from, to)` that
  re-marks `kafka.shadow_tracking` rows pending for a range so the S2 poller re-forwards them via the
  idempotent producer (at-least-once; `to` NULL = to end) — replay now reuses the live outbox path
  rather than a separate engine. **SH-11** deleted the dead, never-instantiated `ReplayEngine` /
  `ReplayQueryBuilder` (whole `replay.rs`) and `ShadowForwarder` / `ForwardMessage` / `ForwardResult`
  (from `forwarder.rs`) + their tests + the mod re-exports, keeping `ForwardDecision`. Net −911/+51 →
  ✅. The shadow E2E `test_replay_historical_messages` now runs its strict path. **All 16 SH findings
  are now resolved** (SH-16 🔵 accepted/freemium).
- 2026-06-30 — re-audit — an independent 8-reader re-verification of `main` (`d7623e0`) **confirmed all 58
  original findings genuinely resolved** (every fix present in code with a fail-before/pass-after
  regression test; report-honesty + regression-discipline spot-checks clean). It surfaced six new
  follow-ups (RA-1..RA-6, see the [Re-audit follow-ups](#re-audit-follow-ups-independent-8-reader-re-verification-2026-06-30)
  section) — most sharing the root cause that the **transactional** shadow-forward path was never moved
  onto the durable outbox (so SH-9/SH-14/SH-15 are fixed only for non-transactional produce), plus a
  BUG-5 produce-panic residual and two outbox-robustness gaps. Initially recorded as 🔴 Open
  (record-only).
- 2026-06-30 — re-audit follow-ups resolved — all six follow-ups (RA-1..RA-6) **fixed** in PRs #62–#65,
  each with a fail-before/pass-after regression test and CI-green self-merge. PR #62 moved the
  transactional commit-forward onto the durable outbox (RA-1 + most of RA-2, no more DB-thread
  `block_on`); #63 added the outbox `retry_count` cap + orphan-row skip (RA-4); #64 routed produce
  `acks≥1` through the panic-safe `dispatch_produce` (RA-3); #65 fenced stale commits on an unknown group
  with `UNKNOWN_MEMBER_ID` and echoed the selected JoinGroup protocol (RA-5 + RA-6). The RA-6 cleanup
  bundle (try_send ack, deleted `sql/shadow_mode.sql`, `is_external_primary` doc, metric over-count,
  `replay_shadow_messages` `txn_state` filter, `PROTOCOL_DEVIATIONS.md` nit) landed across #62/#63/#65.
- 2026-06-30 — post-remediation review — a final independent 5-reader bug + security sweep of `main`
  (`f3e97bc`), with every High/Critical verified firsthand against source. Surfaced **9 new ranked
  findings + a Low bundle** (see the [Post-remediation review](#post-remediation-review-independent-5-reader-bug--security-sweep-2026-06-30)
  section): one **Critical** (RV-1, residual SEC-9 record-batch reserve → unauthenticated SIGABRT), two
  **High** (RV-2 hanging-transaction on producer re-init; RV-3 produce `on_success` outside the
  `catch_unwind`), a **Med-High** (RV-4 read_committed not clamped to LSO), and Meds RV-5..RV-9 — none in
  the original audit or RA re-audit. Recorded 🔴 Open per owner decision (record-only; no fix PRs). The
  transaction subsystem is the least-hardened area; RV-1 is the priority when scheduled.
- 2026-06-30 — second-pass re-review — an independent second 5-reader sweep (SQL injection /
  integer-overflow / concurrency-TOCTOU / dependency-supply-chain + an adversarial refutation pass).
  **RV-1..RV-5 all CONFIRMED** (refutation could not disprove any; RV-5 strengthened via KIP-447). New
  areas swept clean (SQL fully parameterized; single-DB-thread serialization verified real; numeric core
  sound; SEC-5 decompression now confirmed bounded at 256 MiB/batch). Surfaced 5 new Low/Low-Med items
  (see the [Second-pass addendum](#second-pass-addendum-independent-re-review-2026-06-30)): **RV-11**
  `bytes` CVE RUSTSEC-2026-0007 (**fixed in #68**), **RV-12** negative `base_sequence` silent data loss,
  **RV-13** producer-epoch `SMALLINT` overflow, **RV-14** rand/anyhow advisory bumps, **RV-15** latent
  unreachable fork sibling decoders — plus dev-test-only advisories and concurrency latent fragilities.
  Recorded 🔴 Open per owner decision (record-only), except RV-11 which was bumped.
- 2026-07-01 — remediation loop begun — owner reversed the record-only stance: **address every open RV
  finding** via themed PRs (document → address → verify → merge → repeat). A fourth review pass added no
  new High/Critical; it surfaced the flaky-E2E root cause, missing RV regression tests, three doc-drift
  comments, and minor code smells (all folded into the roadmap). PR-1 (test-reliability foundation)
  de-flakes the consumer-group E2E: `test_partition_assignment_strategies` now polls the consumers in a
  retry-`recv` loop (bounded 30 s) until one delivers a record — actively polling drives the rebalance
  and collects the message — replacing the blind 3 s sleep + single 500 ms poll that intermittently
  yielded zero receives (the recurring `coordinator_state.rs` CI flake).
- 2026-07-01 — PR #71 — **RV-1 (Critical) + RV-15 fixed.** The `kafka-protocol` fork now caps the
  RecordBatch record-count `reserve` (`.min(MAX_DECODE_PREALLOC)` — a ~60-byte Produce claiming
  `record_count = i32::MAX` no longer aborts the worker) and guards the four `Vec<u8>`/`String`
  `vec![0; wire_len]` decoders against `buf.remaining()`. Repinned `703af18 → 3094a3e` (built on the
  SEC-9 caps, not the divergent default branch). Fork carries a direct regression test
  (`huge_record_count_errors_instead_of_aborting`); pg_kafka restores the full-pipeline
  `fuzz_parse_record_batch_never_aborts` guard the SEC-9 note had disabled.
- 2026-07-01 — PR #72 — **RV-2 (High) + RV-4 (Med-High) fixed** — the transaction-visibility cluster.
  RV-2: `get_or_create_transactional_producer` now aborts any in-flight transaction (old-epoch `pending`
  records → `aborted`, pending offsets dropped) *before* bumping the epoch, so a producer crash-restart
  no longer strands records / pins the LSO (a hanging transaction). RV-4: the read_committed fetch query
  now clamps to below the LSO (`AND partition_offset < COALESCE(MIN(pending offset), partition_offset+1)`),
  so a committed record at an offset ≥ a still-open lower-offset txn is withheld instead of delivered.
  Regression E2E: `test_reinit_aborts_in_flight_transaction` (asserts the stranded record is aborted +
  no pending rows remain) and `test_read_committed_clamped_to_lso` (interleaved txns; the read_committed
  consumer receives nothing while the lower offset is pending).
- 2026-07-01 — PR #73 — **RV-3 (High) + RV-5 (Med) + RV-12 (Low-Med) fixed** — produce/fencing
  correctness. RV-3: `dispatch_produce` now runs the `on_success` notify hook *inside* the guarded
  handler, so a Postgres ERROR there replies UNKNOWN_SERVER_ERROR + rolls back instead of an unguarded
  panic that hung the client (RA-3 reopened). RV-5: `TxnOffsetCommit` now plumbs the KIP-447
  `generation_id`/`member_id` through the decoder and calls `coordinator.validate_commit`, fencing a
  zombie EOS consumer with `UNKNOWN_MEMBER_ID`/`ILLEGAL_GENERATION` (the producer-epoch fence doesn't
  cover KIP-447); simple/pre-v3 consumers (generation -1) stay exempt. RV-12: `validate_sequence` now
  rejects a negative `base_sequence`/`record_count` (out-of-order) instead of misclassifying it as a
  Duplicate and silently dropping the batch. Regression tests: gated unit tests
  (`test_dispatch_produce_panic_in_on_success_replies_and_resumes`,
  `test_txn_offset_commit_fences_stale_generation`, `test_validate_sequence_negative_base_is_rejected`).
- 2026-07-01 — PR #74 — **RV-7 (Med) + RV-8 (Med) fixed** — transaction lifecycle. RV-7:
  `abort_timed_out_transactions` now enforces each transaction's own `timeout_ms`
  (`COALESCE(timeout_ms, default)`), so a client that set `transaction.timeout.ms > 60 s` is no longer
  force-aborted at 60 s. RV-8: the idempotent producer sequence is no longer advanced inside
  `check_and_update_sequence` (which now only validates) — `handle_produce` calls a new
  `record_producer_sequence` **after** a successful insert, so a failed insert leaves the sequence
  un-advanced and a retry re-inserts instead of being dropped as a false duplicate. Regression tests:
  `test_transaction_honors_per_txn_timeout_ms` (E2E; backdates a txn + waits a sweep cycle) and
  `test_handle_produce_insert_failure_does_not_advance_sequence` (gated; mock fails if the sequence is
  recorded after a failed insert).
- 2026-07-01 — PR #75 — **RV-6 (Med) fixed** — external-primary "readable nowhere". When a shadow
  forward for an external-primary topic exhausts the retry cap (dead-lettered — never delivered
  externally), local reads are now **un-suppressed** so the record is served locally instead of being
  readable nowhere: `suppress_external_primary_reads` returns false once the topic has any dead-lettered
  row (`external_offset IS NULL AND retry_count >= cap`). `apply_ack` logs loudly at the dead-letter
  transition. A new GUC `pg_kafka.shadow_external_primary_strict` (default off) keeps full suppression
  (external-only-strict, accept the loss). Regression E2E `test_external_primary_dead_letter_serves_local`
  (shadow category): a healthy external-primary topic suppresses reads; after forcing a row to the
  dead-letter state the consumer receives the record.
- 2026-07-02 — PR #77 — **RV-13 (Low) + RV-14 (Low) fixed**. RV-13: producer epoch `epoch + 1` overflowed
  the `SMALLINT` column at 32767, failing InitProducerId permanently; now on exhaustion the row's
  `producer_id` is reassigned from the `BIGSERIAL` sequence with the epoch reset to 0, and the
  `transactions.producer_id` FK is made `ON UPDATE CASCADE` so the transaction row follows the new id
  (one row per transactional_id preserved) — matching Kafka's epoch-exhaustion semantics. RV-14: bumped
  `rand` 0.8.5→0.8.6 / 0.9.2→0.9.3 (RUSTSEC-2026-0097) and `anyhow` 1.0.100→1.0.103 (RUSTSEC-2026-0190) in
  `Cargo.lock` (lockfile-only; deps unchanged; Verify-Lockfile validates). Regression E2E
  `test_producer_id_reallocated_on_epoch_exhaustion` drives the epoch to i16::MAX and asserts a new
  producer_id with epoch 0.
- 2026-07-02 — PR #78 — **RV-9 (Med) fixed**. `LengthDelimitedCodec` reserves the full declared frame
  length the moment the 4-byte prefix arrives, so the 1024-connection cap alone bounded reservable memory
  at ~100 GB (1024 × 100 MB `MAX_REQUEST_SIZE`) from a few KB of attacker input. A process-global atomic
  budget (`FRAME_RESERVE_REMAINING`, 512 MiB) now caps *aggregate* in-flight reservations: a new
  `BudgetedFrameCodec` wraps the inner `LengthDelimitedCodec` and, on seeing a frame's declared length,
  charges the global budget *before* the inner codec reserves — admitting the frame only if it fits, else
  returning an `io::Error` that closes the connection; the reservation is released once the frame fully
  decodes (or the decode errors). Individual large frames are still admitted until the budget is hit, so
  legitimate max-size requests are unaffected and `MAX_REQUEST_SIZE` is unchanged. Regression unit test
  `test_frame_reserve_budget_admits_then_rejects_then_releases` (gated `listener.rs`) drives the
  charge/reject/release accounting: admit-to-ceiling, reject-on-overshoot-without-mutation, and
  release-restores-capacity.
- 2026-07-02 — PR #79 — **RV-10 secrets/info sub-bundle fixed** (first of three RV-10 themed PRs). (1)
  `ShadowConfig` derived `Debug`, so a `{:?}` would print the external-broker SASL username/password in
  cleartext (`Config` already redacts; `ShadowConfig` did not). Replaced the derive with a manual `Debug`
  that renders `sasl_username`/`sasl_password` as `[REDACTED]`. (2) `shadow_tracking.error_message` holds
  raw external-broker error strings (authz failures, host/topic names, size limits) yet was readable via
  `GRANT SELECT … TO PUBLIC` (info disclosure, inconsistent with SEC-8). Switched to a column-level grant of
  the six non-sensitive columns, withholding `error_message` from PUBLIC (the bgworker/table owner keeps
  full access). (3) The "idempotence absorbs duplicates" comments (`producer.rs`, `listener.rs`) overstated
  the guarantee — librdkafka idempotence only suppresses duplicates within one producer session, so a
  re-forward across a producer restart still lands externally (at-least-once). Comments corrected.
  Regression unit test `test_debug_redacts_sasl_credentials` (gated `shadow/config.rs`) asserts neither
  credential appears in `{:?}` output.
- 2026-07-02 — PR #80 — **RV-10 CG-conformance sub-bundle fixed** (second of the RV-10 themed PRs). (1)
  JoinGroup never bounded `session_timeout_ms`, so a hostile/misconfigured client could pin a member alive
  for weeks (blocking the group's rebalances) or request a sub-second timeout that thrashes the group. The
  handler now rejects a value outside `[MIN_SESSION_TIMEOUT_MS = 6s, MAX_SESSION_TIMEOUT_MS = 30min]` (Kafka's
  `group.{min,max}.session.timeout.ms` defaults) with `INVALID_SESSION_TIMEOUT` (new error const 26) before
  any group state is created — enforced at the request boundary so the coordinator core can still drive
  expiry with tiny timeouts in unit tests. (2) `validate_commit` accepted an OffsetCommit mid-rebalance;
  it now fences one arriving while the group is `PreparingRebalance`/`CompletingRebalance` with
  `REBALANCE_IN_PROGRESS` (after the generation/member fences, matching Heartbeat and Kafka), so a client
  rejoins before committing rather than persisting an offset for a partition it may be about to lose.
  Regression unit tests: `test_handle_join_group_rejects_out_of_bounds_session_timeout` (gated
  `handlers/tests.rs`) and `test_validate_commit_fenced_during_rebalance` (gated `coordinator.rs`); the
  existing `test_validate_commit_rejects_zombie_commits` now syncs to Stable before asserting the
  current-generation commit is accepted. Follower/leader protocol-name edges from this bullet are deferred
  to the RV-10 consistency PR.
